// Middleware disabled - using page-level protection instead
// This allows more flexible protection without edge runtime issues

export function middleware() {
  // No middleware needed - protection handled at page level
}

export const config = {
  matcher: [],
};
